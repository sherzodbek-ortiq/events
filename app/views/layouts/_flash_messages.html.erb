<% flash.each do |key, value| %>
  <div class="<%= flash_class(key) %>" role="alert">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
      <li>
        <%= value %>
      </li>
    </ul>
  </div>
<% end %>
